import { authStore } from './AuthStore';
import { todoStore } from './TodoStore';
import { themeStore } from './ThemeStore';

export const stores = {
  authStore,
  todoStore,
  themeStore,
};

export { authStore, todoStore, themeStore };
export default stores;
